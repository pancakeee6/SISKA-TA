services:
  backend:
    image: mizzcode/siska-backend:1.0.1
    env_file: ./backend.env
    environment:
      UPLOAD_DIR: /app/uploads
    volumes:
      - /opt/siska/uploads:/app/uploads
    networks:
      front-tier:
        aliases:
          - backend
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == swarm-manager
      restart_policy:
        condition: on-failure

  frontend:
    image: mizzcode/siska-frontend:1.0.1
    networks:
      - front-tier
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == swarm-manager
      restart_policy:
        condition: on-failure

networks:
  front-tier:
    external: true
